import 'package:flutter/material.dart';

class AppColorsLight {
  // bgColor ==> background color
  static final bgColor = Colors.white;
  // tColor ==> teal color
  static final tColor = Colors.teal[800];
  // tdColor ==> teal darker color
  static final tdColor = Colors.teal[900];
  // gaColor ==> green accent color
  static final gaColor = Colors.greenAccent[700];
  // tlColor ==> teal lighter color
  static final tlColor = Colors.teal[200];
  // gColor ==> grey color
  static final gColor = Colors.grey[900];
  // glColor ==> grey lighter color
  static final glColor = Colors.grey[600];
}

class AppColorsDark {
  // bgColor ==> background color
  static final bgColor = Colors.grey[900];
  // bGColor ==> blue grey color
  static final bGColor = Colors.blueGrey[800];
  // taColor ==> teal accent color
  static final taColor = Colors.tealAccent[700];
  // gdColor ==> grey darker color
  static final gdColor = Colors.grey[500];
  // gColor ==> grey color
  static final gColor = Colors.grey[400];
}

